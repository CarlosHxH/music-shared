# ===========================================
# SEPLAG - Template de Variáveis de Ambiente
# ===========================================
# Copie este arquivo para .env e preencha os valores.
# cp .env.example .env
# ===========================================

# --- PostgreSQL ---
POSTGRES_DB=seplag_db
POSTGRES_USER=seplag_user
POSTGRES_PASSWORD=seplag_pass

# --- MinIO ---
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin

# --- JWT (Backend) ---
JWT_SECRET=seplag-secret-key-change-in-production
JWT_EXPIRATION=300000

# --- Frontend (Vite) ---
VITE_API_URL=http://localhost:8080
VITE_WS_URL=http://localhost:8080/ws/albuns

# --- CORS ---
FRONTEND_URL=http://localhost:3000
