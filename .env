# ===========================================
# SEPLAG - Variáveis de Ambiente
# ===========================================
# Copie para .env e ajuste conforme necessário.
# NUNCA commite o .env com credenciais reais.
# ===========================================

# --- PostgreSQL ---
POSTGRES_DB=seplag_db
POSTGRES_USER=seplag_user
POSTGRES_PASSWORD=seplag_pass

# --- MinIO ---
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin

# --- JWT (Backend) ---
# IMPORTANTE: Altere em produção!
JWT_SECRET=seplag-secret-key-change-in-production
JWT_EXPIRATION=300000

# --- Frontend (Vite) ---
# Em Docker: usa /api/v1 (relativo, via nginx proxy)
# Local: usa http://localhost:8080/api/v1
VITE_API_URL=/api/v1
VITE_WS_URL=/ws

# --- CORS / Backend ---
FRONTEND_URL=http://localhost:3000,http://sokoc8cc80wwc8wgoswgg8gw.37.27.45.54.sslip.io:3000
